# Environment Variables Template
# Copy this to .env.local and fill in your actual values
# NEVER commit .env or .env.local to git

# ===== COGNITO AUTHENTICATION =====
VITE_COGNITO_USER_POOL_ID=us-east-1_T5vYoBi0N
VITE_COGNITO_CLIENT_ID=1v5kg2qprjtsnvia0hikm1blvd
VITE_COGNITO_DOMAIN=us-east-1t5vyobi0n.auth.us-east-1.amazoncognito.com

# ===== API GATEWAY =====
VITE_API_GATEWAY_URL=https://plnlaspdy5.execute-api.us-east-1.amazonaws.com/prod

# ===== STRIPE PAYMENTS (Optional) =====
# Get this from Stripe dashboard - it's safe to share (publishable key)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxx

# Safety: do NOT enable real payments unless you have a real booking flow.
# Set to true only when you are actually booking with hotels/partners.
VITE_PAYMENTS_ENABLED=false

# ===== OPTIONAL FEATURES =====
# Force demo mode even if API is available
# VITE_DEMO_MODE=false

# Note: Never commit AWS Secret Access Keys, Lambda functions that use Secrets Manager,
# or Cognito client secrets. These are kept in AWS Secrets Manager instead.
