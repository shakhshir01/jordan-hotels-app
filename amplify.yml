version: 1
applications:
  - appRoot: jordan-hotels-app
    frontend:
      phases:
        preBuild:
          commands:
            - 'if [ -s "$NVM_DIR/nvm.sh" ]; then . "$NVM_DIR/nvm.sh"; nvm install 22.21.1; nvm use 22.21.1; fi'
            - node -v
            - npm ci --include=dev
        build:
          commands:
            - echo "VITE_DISABLE_PRECOMPRESSION=true" >> .env.build
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
